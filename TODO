# TODO vector

#Add enable_if and stuffs
Change in random access iterator the int for difference_type
Add in randon access iterator typedef random_access_iterator iterator;
Check TODO's in every file!

Member functions

# Check this link, talks about diff between const iterators / const vector
http://www.cplusplus.com/forum/general/115538/

#(constructor)
#(destructor)
#operator=

#Iterators:
	#begin
	#end
	#rbegin
	#rend

#Capacity:
	#size
	#max_size
	#resize
	#capacity
	#empty
	#reserve

#Element access:
	#operator[]
	#at
	#front
	#back

#Modifiers:
	#assign
	#push_back
	#pop_back
	#insert
	#erase
	#swap
	#clear

#Allocator:
	#get_allocator

#Non-member function overloads
	#relational operators
	#swap
